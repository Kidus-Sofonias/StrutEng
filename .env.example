# No environment variables are required.
#
# The contact form uses a plain mailto: link addressed to
# sofoniaskidus@gmail.com (see RECIPIENT in src/pages/Contact.jsx) —
# no third-party service, no API keys, completely free.
